# Workspace entrypoint placeholder — not used by Lottery Lab.
# The Lottery Lab app lives in artifacts/lottery-lab/ and is served by:
#   cd artifacts/lottery-lab && uvicorn main:app --host 0.0.0.0 --port $PORT

def main():
    print("Lottery Lab workspace — see artifacts/lottery-lab/")


if __name__ == "__main__":
    main()
