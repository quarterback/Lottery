kind = "web"
previewPath = "/"
title = "Lottery Lab"
version = "1.0.0"
id = "artifacts/lottery-lab"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21381

[services.development]
run = "cd artifacts/lottery-lab && uvicorn main:app --host 0.0.0.0 --port 21381 --reload"
